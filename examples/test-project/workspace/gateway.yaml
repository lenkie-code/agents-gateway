server:
  port: 8000

model:
  default: "gpt-4o-mini"
  temperature: 0.1

auth:
  enabled: false

persistence:
  enabled: true
  backend: sqlite
  url: "sqlite+aiosqlite:///test_project.db"

telemetry:
  enabled: true
  exporter: console
