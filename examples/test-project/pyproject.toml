[project]
name = "test-project"
version = "0.1.0"
description = "Test project for agents-gateway development"
requires-python = ">=3.11"
dependencies = [
    "agents-gateway[postgres,rabbitmq]",
]

[tool.uv.sources]
agents-gateway = { workspace = true }
